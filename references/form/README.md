# C# 表单生成器参考样本

本目录存放 C# 原 FormGeneratorService 生成的 .aspx 表单文件，作为 skill 生成时的参考基准。

## 样本文件

- `信披事项.aspx` — C# 生成器生成的完整表单（包含主表 + 2个明细表 + 隐藏字段）
- `信披事项-config.json` — 对应的表单配置 JSON

## 关键格式规则（从样本提取）

见 SKILL.md 详细文档。
